# 安全审计在区块链是很重要的部分
 想要找到一份区块链工作的话 ，安全审计的部分 必不可少


 重入
    -一般重入
    -跨函数重入
    -跨合约重入
    -原因就是 状态在返回值之后才更改，将状态更改放在外部调用之前更改就行

 DOS
    -返回不正确的格式
    -返回不正确的值
    -链接超时被取消
    -解决办法为 使用try catch 然后重试

 Gas fee
    -gas fee 过大
    -gas fee 全部用完
    -解决办法为 设置limit 过大则拒绝

使用gmx v2 可解决上面问题

